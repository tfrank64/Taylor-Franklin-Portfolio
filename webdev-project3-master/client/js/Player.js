/**************************************************
** GAME PLAYER CLASS
**************************************************/
var Player = function(name) {
	var name = name;
	var id;

	// Define which variables and methods can be accessed
	return {
		name: name,
		id: id
	}
};